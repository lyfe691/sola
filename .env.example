# GitHub API token for the activity feed and changelog (classic, public_repo).
# Optional — without it requests run unauthenticated and rate-limited.
# https://github.com/settings/tokens
GITHUB_TOKEN=
