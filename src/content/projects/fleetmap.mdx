# A delivery fleet, live on an office TV

fleetmap tracks a delivery fleet in real time. Each van's phone posts its GPS position to an API; a screen in the office moves that van's marker, draws its route, and shows how far behind schedule it is. Orders are never typed in. Below: the three data flows the whole system reduces to, why the order sync is a diff instead of a rewrite, and the part that still isn't carrying real drivers.

<ProjectImage
  src="/projects/fleetmap/fleetmap-map.png"
  alt="The fleetmap console: sidebar navigation, a fleet rail listing three vans, and a MapLibre map of northern Switzerland with van markers in Basel, Bern and Zurich"
  caption="The live map: sidebar, fleet rail and map, built for a wall-mounted touchscreen."
  size="large"
/>

## What the console shows

Three views share one layout:

- **Live Map**: every van as a marker, colored by whether it is on route, waiting, behind schedule or stale.
- **Live Tracking**: one van in detail, with distance driven today, current speed, stops left, and the day's itinerary as a numbered timeline of planned pickups and dropoffs.
- **History**: a chosen day replayed from the recorded GPS track, scrubbable and playable at 10x to 120x.

The interface runs in English and Swiss German, in a light or a dark theme.

<ProjectGallery
  images={[
    {
      src: "/projects/fleetmap/fleetmap-map-focus.png",
      alt: "Van Zürich selected: the map has flown to central Zurich, numbered stop badges scattered across the city, and a popover card showing speed, ETA and load",
      caption: "Selecting a van flies the map to its stops.",
    },
    {
      src: "/projects/fleetmap/fleetmap-tracking.png",
      alt: "Live Tracking detail for one van: load and route progress cards, four stat tiles, and a numbered itinerary of scheduled pickups and dropoffs",
      caption: "Live Tracking: load, progress, and the day's itinerary.",
    },
    {
      src: "/projects/fleetmap/fleetmap-history.png",
      alt: "Route Replay: a map of central Zurich with the van's recorded GPS track drawn as a cyan polyline, a playback scrubber at 60x, and distance, duration and GPS point counts",
      caption: "Route Replay, drawn from 41 recorded GPS points.",
    },
  ]}
  columns={3}
/>

## Three data flows, one stateless API

GPS is a push. The phone posts to `POST /api/location`, authenticated as that driver and scoped by row-level security so a van can only be written by its own driver. The handler puts the latest position on the vehicle row and appends to history; Supabase Realtime broadcasts the row change and the dashboard moves the marker. There is no Redis and no WebSocket server of mine. Fan-out is the database's job, which took an entire service out of the deployment.

Orders are a pull. A worker polls the fleet API of the route optimizer the operation already runs, every 60 seconds, and mirrors each rider's routes into `orders` and `stops`. Route lines and ETAs come from `GET /api/route`, a thin proxy in front of OSRM (an open-source routing engine) which I self-host on a Switzerland extract, rather than pay a hosted routing API per request for a screen that recomputes all day.

Driver login is an exchange. The rider app already holds a short-lived token from the optimizer, posts it to an internal service, and gets a Supabase session back after that token is verified upstream and the rider id is mapped to a van. No second driver password exists anywhere.

## Why the order sync is a diff, not a rewrite

The obvious 60-second poll deletes the vehicle's stops and re-inserts them. It works, and it is unusable: every tick emits a Realtime event for every row, the dashboard's route cache (keyed on stop id, sequence and status) misses on all of them, and the map redraws a route that did not change. It is re-printing the whole delivery sheet every minute because one box got ticked.

So the mirror is a single Postgres function that diff-applies the picture for one vehicle. Matched stops are updated only where a column actually differs:

<CodeBlock
  code={`update stops set
  seq    = (s->>'seq')::int,
  status = s->>'status',
  eta_at = nullif(s->>'eta_at', '')::timestamptz
where id = v_stop_id
  and (
    seq    is distinct from (s->>'seq')::int or
    status is distinct from s->>'status' or
    eta_at is distinct from nullif(s->>'eta_at', '')::timestamptz
  );`}
  lang="sql"
/>

Abridged: the real guard covers seven columns. Unchanged rows are never written, so they emit nothing, and stop ids survive across ticks. Stops that vanished from the optimizer's picture are deleted, which makes an empty payload a valid instruction: clear this vehicle.

## Running it on one box

Production is Docker Compose on a single VPS: Caddy terminating TLS in front of the Next.js app, the sync worker, the driver-session service and OSRM, beside a self-hosted Supabase stack. The box has 4 GB of RAM and cannot build images while running both stacks, so all three images are built locally, shipped as one gzipped tar and loaded. The server never builds.

Two decisions only show up after weeks of uptime. GPS history is pruned nightly by pg_cron to 30 days, keyed on server insert time rather than the device timestamp, because a phone with a skewed clock must not decide retention. And nothing reads as late until it is five minutes late, so a minute of traffic doesn't flap the map red.

<ProjectGallery
  images={[
    {
      src: "/projects/fleetmap/fleetmap-gate.png",
      alt: "The entry screen: a Display code heading with a code input and Connect button on the left, and a field of scattered cyan squares on the right",
      caption: "A TV is paired with a display code, not a password.",
    },
    {
      src: "/projects/fleetmap/fleetmap-map-dark.png",
      alt: "The same live map console in dark theme: near-black sidebar and fleet rail with cyan accents, over a dark map style",
      caption: "Dark theme, for a screen that runs overnight.",
    },
  ]}
  columns={2}
/>

The TV never sees a password. It presents a display code, and the server mints a read-only, claim-scoped session from credentials that stay server-side.

## What isn't finished

The passwordless driver exchange is the honest gap. The verification chain was proven end to end from outside on 2026-08-11 with a self-served staging token, and `/api/health` covers the service, but no rider-app build that uses the exchange exists yet, so it is not carrying real drivers. The screenshots here are a local stack with demo data: three vans, and no OSRM process running, which is why the map draws stop markers but no route lines.

The rule that decided most of this build: push the work into something that already exists — the database, the optimizer, the routing engine — and keep the API thin enough that there is nothing in the middle to debug.
